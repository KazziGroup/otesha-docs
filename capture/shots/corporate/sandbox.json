{
  "app": "corporate-portal",
  "page": "corporate/sandbox",
  "note": "Needs the organisation to hold Otesha Connect \u2014 the developer section is gated on a scope, so without it these routes answer 404. Kilima Bank was granted API access from the admin console while writing this module.",
  "figures": [
    {
      "id": "corporate-sandbox-01-en",
      "title": "The sandbox",
      "cropX": false,
      "steps": [
        {
          "goto": "/login"
        },
        {
          "wait": "input[name=phone]"
        },
        {
          "fill": [
            "input[name=phone]",
            "+255717763373"
          ]
        },
        {
          "click": "button[type=submit]"
        },
        {
          "wait": "input[name=code]"
        },
        {
          "eval": "(() => { const p = [...document.querySelectorAll('*')].find(e => e.children.length === 0 && /^\\d{6}$/.test(e.textContent.trim())); if (p) window.__code = p.textContent.trim(); })()"
        },
        {
          "eval": "(() => { const i = document.querySelector('input[name=code]'); const set = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; set.call(i, window.__code); i.dispatchEvent(new Event('input', { bubbles: true })); })()"
        },
        {
          "pause": 600
        },
        {
          "click": "button[type=submit]"
        },
        {
          "wait": "nav"
        },
        {
          "goto": "/f0086ca5-abc2-40fc-8919-e74f91ca5ccb/developers/sandbox"
        },
        {
          "wait": "text=Reset sandbox"
        },
        {
          "pause": 1600
        },
        {
          "park": true
        }
      ],
      "callouts": [
        {
          "text": "Wipes your sandbox and re-seeds it",
          "note": "A known starting point, whenever an experiment has made a mess."
        },
        {
          "text": "not the server",
          "note": "Moving a tree's clock changes your test, not anybody else's data."
        }
      ]
    }
  ]
}
