{
  "app": "corporate-portal",
  "page": "corporate/certificate",
  "note": "Issues a certificate rather than assuming one exists, so the figure rebuilds from an empty database. Each run adds another row for the same period; the crop keeps the newest, and a development database accumulating certificates is the cost of the regeneration guarantee holding.",
  "figures": [
    {
      "id": "corporate-certificate-01-en",
      "title": "A certificate",
      "cropX": false,
      "steps": [
        {
          "goto": "/login"
        },
        {
          "wait": "input[name=phone]"
        },
        {
          "fill": [
            "input[name=phone]",
            "+255717763373"
          ]
        },
        {
          "click": "button[type=submit]"
        },
        {
          "wait": "input[name=code]"
        },
        {
          "eval": "(() => { const p = [...document.querySelectorAll('*')].find(e => e.children.length === 0 && /^\\d{6}$/.test(e.textContent.trim())); if (p) window.__code = p.textContent.trim(); })()"
        },
        {
          "eval": "(() => { const i = document.querySelector('input[name=code]'); const set = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set; set.call(i, window.__code); i.dispatchEvent(new Event('input', { bubbles: true })); })()"
        },
        {
          "pause": 600
        },
        {
          "click": "button[type=submit]"
        },
        {
          "wait": "nav"
        },
        {
          "goto": "/f0086ca5-abc2-40fc-8919-e74f91ca5ccb/reports"
        },
        {
          "wait": "text=Certificates"
        },
        {
          "click": "text=Certificates"
        },
        {
          "wait": "text=Issue for"
        },
        {
          "click": "text=Issue for"
        },
        {
          "wait": "table tbody tr"
        },
        {
          "pause": 2000
        },
        {
          "park": true
        }
      ],
      "callouts": [
        {
          "text": "Issue for",
          "note": "Signs the period set at the top of the page."
        },
        {
          "sel": "table th:nth-child(2)",
          "note": "What it covers: a certificate is a period, signed."
        }
      ]
    }
  ]
}
